
#include "PID.h"

// Set registers, etc to make the PWM values that will correspond with the right speed/thrust outputs to the motors.
// Returns -1 if there was an error (ex: speeds out of range), and 0 otherwise
// TODO: Apply linearizing transformations in the future?
int SetMotorSpeeds(MotorSpeeds speeds) {
  
}
